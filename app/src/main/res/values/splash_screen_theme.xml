<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.BusSplashScreenTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/wave</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="postSplashScreenTheme">@style/Theme.RijekaBusApp</item>
        <item name="background">@color/wave</item>
    </style>
    <style name="ShapeAppearance.MaterialComponents.CircleImageView"
        parent="ShapeAppearance.MaterialComponents">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">?attr/shapeAppearanceSmallComponent</item>
        <!-- Or your custom theme attribute for corner size -->
    </style>
</resources>