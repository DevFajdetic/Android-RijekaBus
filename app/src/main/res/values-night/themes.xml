<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Theme.RijekaBusApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- ANDROID ELEMENTS -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/color_primary_variant</item>
        <!-- Background colors -->
        <item name="android:colorBackground">@color/surface_surface_0</item>

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryVariant">@color/color_primary_variant</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/color_secondary</item>
        <item name="colorSecondaryVariant">@color/color_secondary_variant</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorSurface">@color/surface_surface_1</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="colorOnBackground">@color/black</item>

        <!-- Card colors -->
        <item name="cardBackgroundColor">@color/surface_surface_1</item>
        <item name="cardForegroundColor">@color/black</item>
        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/grey</item>
        <!-- Error colors -->
        <item name="colorError">@color/status_error</item>
        <item name="colorOnError">@color/white</item>
        <!-- Divider colors -->
        <item name="android:divider">@color/surface_surface_2</item>
        <!-- Customize your theme here. -->
        <item name="actionMenuTextColor">@color/white</item>
    </style>

    <style name="BottomSheetDialogTheme" parent="Theme.MaterialComponents.DayNight.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>

    <style name="BottomSheetStyle" parent="Widget.MaterialComponents.BottomSheet.Modal">
        <item name="android:background">@color/surface_surface_1</item>
        <item name="android:backgroundTint">@color/surface_surface_1</item>
        <item name="cardBackgroundColor">@color/surface_surface_1</item>
    </style>

</resources>